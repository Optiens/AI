---
import Layout from '../layouts/Layout.astro';
---

<Layout
  title="自然言語データ検索（デモ） | Optiens"
  description="社内データに自然な日本語で質問できる仕組みのデモ。Optiensの架空販売データ30件に対して、自由な質問でAIが回答します。"
  canonical="https://optiens.com/data-search"
>
  <main class="ds-main">
    <section class="ds-hero">
      <div class="container">
        <h1 class="ds-title">自然言語データ検索</h1>
        <p class="ds-lede">社内データ・社内ドキュメントに、自然な日本語で質問できる仕組みのデモです。<br class="pc-only">下のタブで「販売データ検索」「社内ドキュメント検索」を切り替えられます。</p>
      </div>
    </section>

    <section class="ds-section">
      <div class="container">

        <!-- カテゴリ切替トグル -->
        <div class="ds-category-toggle" role="tablist" aria-label="検索対象の切替">
          <button type="button" class="ds-cat-btn is-active" data-cat="sales" role="tab" aria-selected="true">
            <i class="fa-solid fa-chart-line"></i>
            <span class="ds-cat-name">販売データ検索</span>
            <span class="ds-cat-sub">売上・取引データに質問</span>
          </button>
          <button type="button" class="ds-cat-btn" data-cat="docs" role="tab" aria-selected="false">
            <i class="fa-solid fa-folder-tree"></i>
            <span class="ds-cat-name">社内ドキュメント検索</span>
            <span class="ds-cat-sub">規程・マニュアル・連絡網に質問</span>
          </button>
        </div>

      </div>

      <div class="container ds-container">

        <!-- 左：質問エリア -->
        <div class="ds-query-panel">
          <h2 class="ds-panel-title">質問する</h2>
          <p class="ds-panel-desc">下のサンプルから選ぶか、自由に入力できます。</p>

          <!-- 販売データ用サンプル -->
          <div class="ds-samples" data-cat-show="sales">
            <p class="ds-samples-group-label">集計</p>
            <button class="ds-sample" data-q="2026年 4 月の売上合計はいくら？">2026年 4 月の売上合計は？</button>
            <button class="ds-sample" data-q="士業のお客様の合計売上と件数を教えて">士業のお客様の合計売上と件数</button>
            <button class="ds-sample" data-q="業種別の売上比率を教えて">業種別の売上比率</button>
            <button class="ds-sample" data-q="保守プランを契約している顧客の一覧と月額合計">保守プラン契約顧客と月額合計</button>

            <p class="ds-samples-group-label">解析・分析</p>
            <button class="ds-sample" data-q="改善が必要な業種はどこ？取引件数や単価から分析してください">改善が必要な業種は？</button>
            <button class="ds-sample" data-q="売上が伸びている領域はどこ？月次推移と業種から見てください">売上が伸びている領域は？</button>
            <button class="ds-sample" data-q="リピート購入してくれた顧客の傾向を教えて。共通点があれば指摘してください">リピート顧客の傾向と共通点</button>
            <button class="ds-sample" data-q="無料の簡易レポートから有償サービスへ繋がりやすい業種は？">有償化に繋がりやすい業種は？</button>
          </div>

          <!-- 社内ドキュメント用サンプル -->
          <div class="ds-samples" data-cat-show="docs" hidden>
            <p class="ds-samples-group-label">規程・手続き</p>
            <button class="ds-sample" data-q="経費精算ってどうやるの？">経費精算ってどうやるの？</button>
            <button class="ds-sample" data-q="有給って何日もらえる？">有給って何日もらえる？</button>
            <button class="ds-sample" data-q="リモートワークしたい時の手続きは？">リモートワークの手続きは？</button>

            <p class="ds-samples-group-label">業務マニュアル</p>
            <button class="ds-sample" data-q="クレームが入った時の対応方法は？">クレーム対応の方法は？</button>
            <button class="ds-sample" data-q="新人研修のスケジュールは？">新人研修のスケジュールは？</button>

            <p class="ds-samples-group-label">連絡網・システム</p>
            <button class="ds-sample" data-q="金田部長の連絡先教えて">金田部長の連絡先教えて</button>
            <button class="ds-sample" data-q="freee のログイン情報どこ？">freee のログイン情報どこ？</button>
            <button class="ds-sample" data-q="評価面談の準備は何をすればいい？">評価面談の準備は？</button>
          </div>

          <form id="ds-form" class="ds-form">
            <textarea
              id="ds-input"
              class="ds-input"
              rows="3"
              placeholder="例: 先月の売上で一番大きな案件は？"
              maxlength="200"
            ></textarea>
            <button type="submit" id="ds-submit" class="ds-submit">
              <i class="fa-solid fa-magnifying-glass"></i> AI に質問
            </button>
            <span id="ds-status" class="ds-status"></span>
          </form>
        </div>

        <!-- 右：回答エリア -->
        <div class="ds-answer-panel">
          <h2 class="ds-panel-title">AI の回答</h2>
          <div id="ds-answer-box" class="ds-answer-box ds-answer-empty">
            <p class="ds-answer-placeholder">左側で質問を入力すると、ここに AI の回答が表示されます。</p>
          </div>

          <h3 class="ds-section-sub" id="ds-matched-title" style="display:none;">該当データ</h3>
          <div id="ds-matched-table" class="ds-matched-wrap"></div>
        </div>

      </div>

      <!-- 注記 -->
      <div class="container">
        <div class="ds-footnote">
          <p><strong>このデモについて</strong>: 表示しているデータはすべて<strong>サンプルデータ</strong>です。実際のお客様向けには、御社の業務データ（CRM・売上・在庫・問い合わせ履歴など）に対して、同じ仕組みで対話的に問い合わせできる画面をご提供します。</p>
          <p><strong>弊社での運用</strong>: Optiens 社内では、水耕栽培の収穫データ・電力消費データに対して、同じ仕組みで対話分析しています。</p>
          <a href="/ai-examples#service-search" class="ds-back">← AI業務自動化デモに戻る</a>
        </div>
      </div>
    </section>

    <!-- スケール / コスト -->
    <section class="ds-section">
      <div class="container">
        <div class="ds-scale-card">
          <div class="ds-scale-head">
            <i class="fa-solid fa-database"></i>
            <h2 class="ds-scale-title">データ件数・コスト感</h2>
          </div>
          <p class="ds-scale-lede">
            データ規模に応じてアーキテクチャを切り替えます。御社の保有件数を伺った上で最適な構成をご提案します。
          </p>

          <div class="ds-scale-table-wrap">
            <table class="ds-scale-table">
              <thead>
                <tr>
                  <th>データ件数</th>
                  <th>採用方式</th>
                  <th>応答速度の目安</th>
                  <th>1 クエリあたりコスト目安</th>
                </tr>
              </thead>
              <tbody>
                <tr>
                  <td><strong>〜2,000 件</strong></td>
                  <td>本デモと同じ <strong>CSV 直送</strong>方式（全件を AI に渡す）</td>
                  <td>3〜8 秒</td>
                  <td>約 ¥0.5〜2</td>
                </tr>
                <tr>
                  <td><strong>2,000〜数百万件</strong></td>
                  <td><strong>Text-to-SQL</strong>（AI が SQL を生成 → DB で集計 → 結果を要約）</td>
                  <td>2〜5 秒</td>
                  <td>約 ¥1〜3</td>
                </tr>
                <tr>
                  <td><strong>類似検索が中心の場合</strong></td>
                  <td><strong>RAG / pgvector</strong>（埋め込みベクトルで関連件のみ抽出）</td>
                  <td>2〜4 秒</td>
                  <td>約 ¥1〜3<br /><small>＋ 初期埋め込み 数百〜数千円（1 回のみ）</small></td>
                </tr>
                <tr>
                  <td><strong>用途が混在する場合</strong></td>
                  <td><strong>ハイブリッド</strong>（集計は SQL、自由検索は RAG に振り分け）</td>
                  <td>2〜6 秒</td>
                  <td>約 ¥1〜4</td>
                </tr>
              </tbody>
            </table>
          </div>

          <div class="ds-scale-monthly">
            <h3 class="ds-scale-monthly-title">月額ランニングコスト試算（参考）</h3>
            <ul class="ds-scale-monthly-list">
              <li><strong>1 日 10 クエリ</strong>（小規模社内利用）: AI API 月額 <strong>¥300〜3,000</strong> 程度</li>
              <li><strong>1 日 100 クエリ</strong>（部署単位で日常利用）: AI API 月額 <strong>¥3,000〜30,000</strong> 程度</li>
              <li><strong>1 日 1,000 クエリ</strong>（全社・常用）: AI API 月額 <strong>¥30,000〜300,000</strong> 程度</li>
            </ul>
            <p class="ds-scale-monthly-note">※ 上記は <strong>AI API 利用料のみの試算</strong>です。下限は gpt-4o-mini 等の軽量モデル利用時、上限は <strong>Claude Opus 4.7 / GPT-5.5</strong> 等の最新フラッグシップ＋長文プロンプト時の目安。Supabase 等の DB 利用料、月額保守費は別途。<br />※ Text-to-SQL や RAG では精度が業務影響に直結するため、<strong>バランスモデル（Claude Sonnet 4.6 / GPT-5.4）以上を推奨</strong>。コストは中央値帯（¥3,000〜30,000/月）に収まることが多いです。<br />※ ローカル LLM 運用を選択された場合は API 利用料はかかりません（GPU 設備または GPU クラウドレンタル代が別途発生します）。<br />※ 2026-05 時点の料金。AI モデル料金は四半期単位で改定される可能性があります。</p>
          </div>

          <p class="ds-scale-cta">
            御社のデータ規模・想定利用頻度から、最適な方式と費用感を <a href="/free-diagnosis">無料 AI 活用診断</a> でご提案します。
          </p>
        </div>
      </div>
    </section>

    <!-- データプレビュー -->
    <section class="ds-section">
      <div class="container">
        <details class="ds-data-preview">
          <summary id="ds-all-table-label">サンプル販売データをすべて見る</summary>
          <div id="ds-all-table" class="ds-table-wrap"></div>
        </details>
      </div>
    </section>
  </main>
</Layout>

<script>
type SalesRecord = {
  id: string
  date: string
  client: string
  service: string
  industry: string
  region: string
  amount: number
  status: 'completed' | 'in-progress' | 'cancelled'
}

type InternalDoc = {
  id: string
  title: string
  category: string
  excerpt: string
  body: string
  updatedAt: string
}

type Category = 'sales' | 'docs'

const form = document.getElementById('ds-form') as HTMLFormElement
const input = document.getElementById('ds-input') as HTMLTextAreaElement
const submit = document.getElementById('ds-submit') as HTMLButtonElement
const statusEl = document.getElementById('ds-status') as HTMLElement
const answerBox = document.getElementById('ds-answer-box') as HTMLElement
const matchedTitle = document.getElementById('ds-matched-title') as HTMLElement
const matchedTable = document.getElementById('ds-matched-table') as HTMLElement
const allTable = document.getElementById('ds-all-table') as HTMLElement
const allTableLabel = document.getElementById('ds-all-table-label') as HTMLElement | null

let currentCat: Category = 'sales'
let allRecords: SalesRecord[] = []
let allDocs: InternalDoc[] = []

// サンプルデータ取得（販売）
async function loadAllRecords() {
  try {
    const res = await fetch('/api/data-search-demo?category=sales')
    if (!res.ok) return
    const data = await res.json()
    allRecords = data.records || []
    if (currentCat === 'sales') renderAllTable()
  } catch (err) {
    console.error('Failed to load sales records:', err)
  }
}

// サンプルデータ取得（社内ドキュメント）
async function loadAllDocs() {
  try {
    const res = await fetch('/api/data-search-demo?category=docs')
    if (!res.ok) return
    const data = await res.json()
    allDocs = data.records || []
    if (currentCat === 'docs') renderAllTable()
  } catch (err) {
    console.error('Failed to load docs:', err)
  }
}

function fmtAmount(n: number) {
  return '¥' + n.toLocaleString('ja-JP')
}

function statusLabel(s: string) {
  if (s === 'completed') return '完了'
  if (s === 'in-progress') return '進行中'
  if (s === 'cancelled') return 'キャンセル'
  return s
}

function renderTable(records: SalesRecord[], target: HTMLElement) {
  if (records.length === 0) {
    target.innerHTML = '<p class="ds-empty">該当する記録はありません。</p>'
    return
  }
  const rows = records.map(r => `
    <tr>
      <td class="ds-cell-id">${r.id}</td>
      <td>${r.date}</td>
      <td class="ds-cell-client">${r.client}</td>
      <td>${r.service}</td>
      <td><span class="ds-tag">${r.industry}</span></td>
      <td>${r.region}</td>
      <td class="ds-cell-amount">${fmtAmount(r.amount)}</td>
      <td><span class="ds-status-badge ds-status-${r.status}">${statusLabel(r.status)}</span></td>
    </tr>
  `).join('')
  target.innerHTML = `
    <table class="ds-table">
      <thead>
        <tr>
          <th>ID</th><th>日付</th><th>顧客</th><th>サービス</th>
          <th>業種</th><th>地域</th><th class="ds-cell-amount">金額</th><th>状態</th>
        </tr>
      </thead>
      <tbody>${rows}</tbody>
    </table>
  `
}

function renderDocsTable(records: InternalDoc[], target: HTMLElement) {
  if (records.length === 0) {
    target.innerHTML = '<p class="ds-empty">該当するドキュメントはありません。</p>'
    return
  }
  const rows = records.map(d => `
    <tr>
      <td class="ds-cell-id">${d.id}</td>
      <td class="ds-cell-client">${escapeHtml(d.title)}</td>
      <td><span class="ds-tag">${escapeHtml(d.category)}</span></td>
      <td>${escapeHtml(d.excerpt)}</td>
      <td class="ds-cell-id">${d.updatedAt}</td>
    </tr>
  `).join('')
  target.innerHTML = `
    <table class="ds-table">
      <thead>
        <tr>
          <th>ID</th><th>タイトル</th><th>カテゴリ</th><th>概要</th><th>最終更新</th>
        </tr>
      </thead>
      <tbody>${rows}</tbody>
    </table>
  `
}

function renderAllTable() {
  if (currentCat === 'sales') {
    if (allTableLabel) allTableLabel.textContent = `サンプル販売データをすべて見る（${allRecords.length}件）`
    renderTable(allRecords, allTable)
  } else {
    if (allTableLabel) allTableLabel.textContent = `サンプル社内ドキュメントをすべて見る（${allDocs.length}件）`
    renderDocsTable(allDocs, allTable)
  }
}

function renderMatched(matchedIds: string[]) {
  if (matchedIds.length === 0) {
    matchedTitle.style.display = 'none'
    matchedTable.innerHTML = ''
    return
  }
  if (currentCat === 'sales') {
    const ids = new Set(matchedIds)
    const matched = allRecords.filter(r => ids.has(r.id))
    if (matched.length > 0) {
      matchedTitle.style.display = 'block'
      matchedTitle.textContent = '該当する販売データ'
      renderTable(matched, matchedTable)
    }
  } else {
    const ids = new Set(matchedIds)
    const matched = allDocs.filter(d => ids.has(d.id))
    if (matched.length > 0) {
      matchedTitle.style.display = 'block'
      matchedTitle.textContent = '引用元ドキュメント'
      renderDocsTable(matched, matchedTable)
    }
  }
}

function escapeHtml(s: string) {
  return s.replace(/[&<>"']/g, c => ({ '&': '&amp;', '<': '&lt;', '>': '&gt;', '"': '&quot;', "'": '&#39;' }[c] as string))
}

// 簡易 Markdown レンダラ（見出し / 太字 / リスト / 表）
function renderMarkdown(md: string): string {
  let html = escapeHtml(md)
  html = html.replace(/\*\*(.+?)\*\*/g, '<strong>$1</strong>')

  // 表
  const lines = html.split('\n')
  const out: string[] = []
  let i = 0
  while (i < lines.length) {
    const line = lines[i] ?? ''
    if (line.match(/^\|.+\|$/) && lines[i + 1]?.match(/^\|[\s\-:|]+\|$/)) {
      // 表開始
      const headers = line.split('|').slice(1, -1).map(s => s.trim())
      i += 2
      const tableRows: string[] = []
      while (i < lines.length && (lines[i] ?? '').match(/^\|.+\|$/)) {
        const rowLine = lines[i] ?? ''
        const cells = rowLine.split('|').slice(1, -1).map(s => s.trim())
        tableRows.push('<tr>' + cells.map(c => `<td>${c}</td>`).join('') + '</tr>')
        i++
      }
      out.push(`<table class="ds-md-table"><thead><tr>${headers.map(h => `<th>${h}</th>`).join('')}</tr></thead><tbody>${tableRows.join('')}</tbody></table>`)
      continue
    }
    if (line.match(/^- /)) {
      // リスト
      const items: string[] = []
      while (i < lines.length && (lines[i] ?? '').match(/^- /)) {
        items.push(`<li>${(lines[i] ?? '').replace(/^- /, '')}</li>`)
        i++
      }
      out.push('<ul>' + items.join('') + '</ul>')
      continue
    }
    if (line.trim() === '') {
      i++
      continue
    }
    out.push(`<p>${line}</p>`)
    i++
  }
  return out.join('\n')
}

async function ask(query: string) {
  if (!query.trim()) return
  submit.disabled = true
  statusEl.textContent = 'AI が考えています…'
  answerBox.classList.remove('ds-answer-empty')
  answerBox.innerHTML = '<p class="ds-loading"><i class="fa-solid fa-spinner fa-spin"></i> 回答を生成中…</p>'
  matchedTitle.style.display = 'none'
  matchedTable.innerHTML = ''

  try {
    const res = await fetch('/api/data-search-demo', {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({ query, category: currentCat }),
    })
    const data = await res.json()
    if (!res.ok) {
      answerBox.innerHTML = `<p class="ds-error">${escapeHtml(data?.error || 'エラーが発生しました')}</p>`
      statusEl.textContent = ''
      return
    }

    const answerHtml = renderMarkdown(data.answer || '')
    let calc = ''
    if (data.calculation_note) {
      calc = `<details class="ds-calc"><summary>計算根拠</summary><pre>${escapeHtml(data.calculation_note)}</pre></details>`
    }
    let mockNote = ''
    if (data.mock && currentCat === 'docs') {
      mockNote = '<p class="ds-mock-note">※ デモのため事前用意のサンプル応答を表示しています。実運用時は AI が御社の社内文書全件をベクトル検索して動的に回答します（AI API の<strong>従量課金</strong>が発生）。</p>'
    } else if (data.mock) {
      mockNote = '<p class="ds-mock-note">※ モック応答です（API キー未設定環境）</p>'
    }
    answerBox.innerHTML = `${mockNote}<div class="ds-answer-content">${answerHtml}</div>${calc}`

    // 該当データ表示
    if (Array.isArray(data.matched_ids) && data.matched_ids.length > 0) {
      renderMatched(data.matched_ids)
    } else {
      matchedTitle.style.display = 'none'
      matchedTable.innerHTML = ''
    }

    statusEl.textContent = data.remaining !== undefined ? `残り ${data.remaining} 回 / 1時間` : ''
  } catch (err) {
    answerBox.innerHTML = '<p class="ds-error">通信エラーが発生しました。</p>'
    statusEl.textContent = ''
  } finally {
    submit.disabled = false
  }
}

// カテゴリ切替
function switchCategory(cat: Category) {
  currentCat = cat
  document.querySelectorAll<HTMLButtonElement>('.ds-cat-btn').forEach((b) => {
    const isActive = b.dataset.cat === cat
    b.classList.toggle('is-active', isActive)
    b.setAttribute('aria-selected', isActive ? 'true' : 'false')
  })
  document.querySelectorAll<HTMLElement>('.ds-samples').forEach((s) => {
    s.hidden = s.dataset.catShow !== cat
  })
  // プレースホルダーと placeholder を変更
  if (cat === 'docs') {
    input.placeholder = '例: 経費精算ってどうやるの？'
  } else {
    input.placeholder = '例: 先月の売上で一番大きな案件は？'
  }
  // 回答エリアをリセット
  answerBox.classList.add('ds-answer-empty')
  answerBox.innerHTML = '<p class="ds-answer-placeholder">左側で質問を入力すると、ここに AI の回答が表示されます。</p>'
  matchedTitle.style.display = 'none'
  matchedTable.innerHTML = ''
  input.value = ''
  // テーブル再描画
  renderAllTable()
}

document.querySelectorAll<HTMLButtonElement>('.ds-cat-btn').forEach((btn) => {
  btn.addEventListener('click', () => {
    switchCategory(btn.dataset.cat as Category)
  })
})

// イベント
form.addEventListener('submit', (e) => {
  e.preventDefault()
  ask(input.value)
})

document.querySelectorAll('.ds-sample').forEach((btn) => {
  btn.addEventListener('click', (e) => {
    const q = (e.currentTarget as HTMLButtonElement).getAttribute('data-q') || ''
    input.value = q
    ask(q)
  })
})

loadAllRecords()
loadAllDocs()
</script>

<style>
  .ds-main {
    background: #F8FAFD;
  }
  .ds-hero {
    padding: clamp(96px, 12vw, 140px) 0 clamp(20px, 3vw, 32px);
    background: #fff;
    border-bottom: 1px solid rgba(31, 58, 147, 0.08);
  }
  .ds-eyebrow {
    font-size: 0.78rem;
    color: #6B85C9;
    letter-spacing: 0.16em;
    font-weight: 700;
    margin: 0 0 8px;
  }
  .ds-title {
    font-size: clamp(1.6rem, 3.4vw, 2.2rem);
    font-weight: 800;
    color: #0f172a;
    margin: 0 0 12px;
    letter-spacing: -.01em;
  }
  .ds-lede {
    color: #64748b;
    font-size: clamp(0.9rem, 1.4vw, 1rem);
    line-height: 1.8;
    margin: 0;
  }

  .ds-section {
    padding: clamp(28px, 4vw, 48px) 0;
  }
  .ds-section:last-of-type {
    padding-bottom: clamp(80px, 10vw, 120px);
  }

  /* カテゴリ切替トグル */
  .ds-category-toggle {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 12px;
    margin-bottom: 22px;
  }
  .ds-cat-btn {
    display: flex;
    align-items: center;
    gap: 14px;
    padding: 16px 20px;
    background: #fff;
    border: 2px solid rgba(31, 58, 147, 0.12);
    border-radius: 12px;
    cursor: pointer;
    text-align: left;
    transition: border-color 0.2s, transform 0.1s, background 0.2s;
    font-family: inherit;
  }
  .ds-cat-btn:hover { border-color: rgba(31, 58, 147, 0.30); }
  .ds-cat-btn.is-active {
    border-color: #1F3A93;
    background: linear-gradient(135deg, rgba(31, 58, 147, 0.05) 0%, rgba(228, 138, 149, 0.04) 100%);
  }
  .ds-cat-btn i {
    font-size: 1.2rem;
    width: 40px; height: 40px;
    border-radius: 10px;
    background: rgba(31, 58, 147, 0.10);
    color: #1F3A93;
    display: inline-flex;
    align-items: center;
    justify-content: center;
    flex-shrink: 0;
  }
  .ds-cat-name {
    display: block;
    font-weight: 700;
    color: #0f172a;
    font-size: 0.95rem;
  }
  .ds-cat-sub {
    display: block;
    font-size: 0.74rem;
    color: #64748b;
    margin-top: 2px;
  }
  @media (max-width: 640px) {
    .ds-category-toggle { grid-template-columns: 1fr; }
  }

  .ds-container {
    display: grid;
    grid-template-columns: minmax(0, 1fr) minmax(0, 1.4fr);
    gap: 24px;
    margin-bottom: 24px;
  }
  .ds-panel-title {
    font-size: 0.95rem;
    font-weight: 700;
    color: #0f172a;
    margin: 0 0 8px;
  }
  .ds-panel-desc {
    font-size: 0.82rem;
    color: #64748b;
    margin: 0 0 14px;
  }

  /* 質問パネル */
  .ds-query-panel,
  .ds-answer-panel {
    background: #fff;
    border: 1px solid rgba(31, 58, 147, 0.10);
    border-radius: 12px;
    padding: 22px 24px;
  }
  .ds-samples {
    display: flex;
    flex-direction: column;
    gap: 8px;
    margin-bottom: 18px;
  }
  .ds-samples-group-label {
    margin: 6px 0 0;
    font-size: 0.72rem;
    font-weight: 700;
    color: #6B85C9;
    letter-spacing: 0.08em;
  }
  .ds-samples-group-label:first-child {
    margin-top: 0;
  }
  .ds-sample {
    text-align: left;
    padding: 10px 14px;
    border-radius: 8px;
    border: 1px solid rgba(31, 58, 147, 0.12);
    background: #F8FAFD;
    color: #0f172a;
    font-size: 0.85rem;
    cursor: pointer;
    transition: background 0.2s ease, border-color 0.2s ease;
  }
  .ds-sample:hover {
    background: rgba(31, 58, 147, 0.06);
    border-color: rgba(31, 58, 147, 0.30);
  }
  .ds-form {
    display: flex;
    flex-direction: column;
    gap: 10px;
  }
  .ds-input {
    width: 100%;
    padding: 12px 14px;
    border: 1px solid rgba(31, 58, 147, 0.20);
    border-radius: 8px;
    font-size: 0.9rem;
    line-height: 1.6;
    resize: vertical;
    font-family: inherit;
  }
  .ds-input:focus {
    outline: none;
    border-color: #1F3A93;
    box-shadow: 0 0 0 3px rgba(31, 58, 147, 0.10);
  }
  .ds-submit {
    padding: 12px 20px;
    background: linear-gradient(135deg, #1F3A93 0%, #6B85C9 100%);
    color: #fff;
    border: none;
    border-radius: 999px;
    font-weight: 700;
    font-size: 0.9rem;
    cursor: pointer;
    align-self: flex-start;
    transition: transform 0.15s ease, box-shadow 0.15s ease;
  }
  .ds-submit:hover {
    transform: translateY(-1px);
    box-shadow: 0 6px 16px rgba(31, 58, 147, 0.30);
  }
  .ds-submit:disabled {
    opacity: 0.6;
    cursor: not-allowed;
    transform: none;
  }
  .ds-status {
    font-size: 0.78rem;
    color: #94A3B8;
  }

  /* 回答 */
  .ds-answer-box {
    min-height: 180px;
    background: #F8FAFD;
    border: 1px solid rgba(31, 58, 147, 0.06);
    border-radius: 10px;
    padding: 18px;
    color: #0f172a;
    font-size: 0.9rem;
    line-height: 1.8;
    margin-bottom: 18px;
  }
  .ds-answer-empty {
    display: flex;
    align-items: center;
    justify-content: center;
  }
  .ds-answer-placeholder {
    color: #94A3B8;
    margin: 0;
    text-align: center;
    font-size: 0.85rem;
  }
  .ds-loading {
    color: #6B85C9;
    margin: 0;
  }
  .ds-error {
    color: #B91C1C;
    margin: 0;
  }
  .ds-mock-note {
    color: #94A3B8;
    font-size: 0.78rem;
    margin: 0 0 10px;
  }
  .ds-answer-content p { margin: 0 0 10px; }
  .ds-answer-content p:last-child { margin-bottom: 0; }
  .ds-answer-content ul {
    margin: 8px 0;
    padding-left: 20px;
  }
  .ds-answer-content li { margin-bottom: 4px; }
  .ds-md-table {
    width: 100%;
    border-collapse: collapse;
    margin: 10px 0;
    font-size: 0.85rem;
  }
  .ds-md-table th, .ds-md-table td {
    padding: 6px 10px;
    border: 1px solid rgba(31, 58, 147, 0.15);
    text-align: left;
  }
  .ds-md-table th {
    background: rgba(31, 58, 147, 0.06);
    font-weight: 700;
  }
  .ds-calc {
    margin-top: 10px;
    padding: 8px 10px;
    background: rgba(31, 58, 147, 0.04);
    border-radius: 6px;
  }
  .ds-calc summary {
    font-size: 0.78rem;
    color: #6B85C9;
    cursor: pointer;
    font-weight: 600;
  }
  .ds-calc pre {
    margin: 8px 0 0;
    font-size: 0.78rem;
    color: #475569;
    white-space: pre-wrap;
    font-family: inherit;
  }

  .ds-section-sub {
    font-size: 0.85rem;
    font-weight: 700;
    color: #0f172a;
    margin: 0 0 10px;
  }

  /* テーブル */
  .ds-matched-wrap, .ds-table-wrap {
    overflow-x: auto;
    background: #fff;
    border-radius: 8px;
  }
  .ds-table {
    width: 100%;
    border-collapse: collapse;
    font-size: 0.8rem;
  }
  .ds-table th, .ds-table td {
    padding: 8px 10px;
    border-bottom: 1px solid rgba(31, 58, 147, 0.08);
    text-align: left;
    white-space: nowrap;
  }
  .ds-table th {
    background: rgba(31, 58, 147, 0.04);
    color: #475569;
    font-weight: 700;
    font-size: 0.75rem;
  }
  .ds-cell-id {
    font-family: 'Inter', monospace;
    color: #94A3B8;
    font-size: 0.75rem;
  }
  .ds-cell-client { font-weight: 600; }
  .ds-cell-amount { text-align: right; font-family: 'Inter', monospace; }
  .ds-tag {
    display: inline-block;
    padding: 2px 8px;
    background: rgba(31, 58, 147, 0.08);
    color: #1F3A93;
    border-radius: 999px;
    font-size: 0.72rem;
    font-weight: 600;
  }
  .ds-status-badge {
    display: inline-block;
    padding: 2px 8px;
    border-radius: 999px;
    font-size: 0.7rem;
    font-weight: 700;
  }
  .ds-status-completed { background: rgba(16, 185, 129, 0.12); color: #047857; }
  .ds-status-in-progress { background: rgba(31, 58, 147, 0.10); color: #1F3A93; }
  .ds-status-cancelled { background: rgba(100, 116, 139, 0.15); color: #475569; }

  /* スケール・コスト */
  .ds-scale-card {
    background: linear-gradient(135deg, rgba(31, 58, 147, 0.04) 0%, rgba(107, 133, 201, 0.03) 60%, rgba(228, 138, 149, 0.03) 100%);
    border: 1px solid rgba(31, 58, 147, 0.15);
    border-radius: 14px;
    padding: 28px clamp(20px, 4vw, 36px);
  }
  .ds-scale-head {
    display: flex;
    align-items: center;
    gap: 12px;
    margin-bottom: 8px;
  }
  .ds-scale-head i {
    font-size: 1.1rem;
    color: #1F3A93;
    background: rgba(31, 58, 147, 0.10);
    width: 40px; height: 40px;
    border-radius: 10px;
    display: inline-flex;
    align-items: center;
    justify-content: center;
  }
  .ds-scale-title {
    font-size: 1.05rem;
    font-weight: 700;
    color: #0f172a;
    margin: 0;
  }
  .ds-scale-lede {
    font-size: 0.88rem;
    color: #64748b;
    line-height: 1.8;
    margin: 0 0 18px;
  }
  .ds-scale-table-wrap {
    overflow-x: auto;
    background: #fff;
    border-radius: 10px;
    border: 1px solid rgba(31, 58, 147, 0.10);
    margin-bottom: 22px;
  }
  .ds-scale-table {
    width: 100%;
    border-collapse: collapse;
    font-size: 0.85rem;
  }
  .ds-scale-table th,
  .ds-scale-table td {
    padding: 12px 14px;
    border-bottom: 1px solid rgba(31, 58, 147, 0.08);
    text-align: left;
    color: #334155;
    line-height: 1.7;
  }
  .ds-scale-table th {
    background: rgba(31, 58, 147, 0.04);
    color: #475569;
    font-weight: 700;
    font-size: 0.78rem;
    white-space: nowrap;
  }
  .ds-scale-table td strong { color: #0f172a; }
  .ds-scale-table td small { color: #94A3B8; font-size: 0.78em; }
  .ds-scale-monthly {
    padding: 16px 18px;
    background: #fff;
    border: 1px solid rgba(31, 58, 147, 0.10);
    border-radius: 10px;
    margin-bottom: 14px;
  }
  .ds-scale-monthly-title {
    font-size: 0.92rem;
    font-weight: 700;
    color: #0f172a;
    margin: 0 0 10px;
  }
  .ds-scale-monthly-list {
    margin: 0 0 12px;
    padding-left: 20px;
  }
  .ds-scale-monthly-list li {
    font-size: 0.85rem;
    color: #334155;
    line-height: 1.85;
  }
  .ds-scale-monthly-list strong { color: #1F3A93; }
  .ds-scale-monthly-note {
    font-size: 0.78rem;
    color: #64748b;
    line-height: 1.8;
    margin: 12px 0 0;
    padding-top: 10px;
    border-top: 1px dashed rgba(31, 58, 147, 0.15);
  }
  .ds-scale-monthly-note strong { color: #475569; }
  .ds-scale-cta {
    margin: 0;
    text-align: center;
    font-size: 0.88rem;
    color: #475569;
    padding-top: 4px;
  }
  .ds-scale-cta a {
    color: #1F3A93;
    font-weight: 700;
    text-decoration: none;
  }
  .ds-scale-cta a:hover { text-decoration: underline; }

  .ds-data-preview {
    background: #fff;
    border: 1px solid rgba(31, 58, 147, 0.10);
    border-radius: 12px;
    padding: 14px 18px;
  }
  .ds-data-preview summary {
    cursor: pointer;
    font-size: 0.85rem;
    font-weight: 700;
    color: #1F3A93;
    list-style: none;
  }
  .ds-data-preview summary::before {
    content: '▶';
    display: inline-block;
    margin-right: 8px;
    font-size: 0.7rem;
    transition: transform 0.2s ease;
  }
  .ds-data-preview[open] summary::before {
    transform: rotate(90deg);
  }
  .ds-data-preview[open] summary {
    margin-bottom: 12px;
  }

  .ds-empty {
    color: #94A3B8;
    font-size: 0.85rem;
    margin: 0;
    padding: 12px 0;
  }

  /* Footnote */
  .ds-footnote {
    background: #fff;
    border: 1px dashed rgba(31, 58, 147, 0.20);
    border-radius: 12px;
    padding: 18px 22px;
  }
  .ds-footnote p {
    font-size: 0.85rem;
    color: #475569;
    line-height: 1.8;
    margin: 0 0 10px;
  }
  .ds-footnote p:last-of-type { margin-bottom: 14px; }
  .ds-footnote strong { color: #0f172a; }
  .ds-back {
    display: inline-block;
    color: #1F3A93;
    font-weight: 700;
    text-decoration: none;
    font-size: 0.85rem;
  }
  .ds-back:hover { text-decoration: underline; }

  @media (max-width: 900px) {
    .ds-container { grid-template-columns: 1fr; }
    .pc-only { display: none; }
  }
</style>
